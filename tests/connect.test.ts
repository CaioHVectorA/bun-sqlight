import { describe, test, expect } from 'bun:test'
import { BunSqlight } from '../src/lib/connect'

describe("Connection to SQLite DB", () => {
    test.todo('Should be able to connect to a database')
    test.todo('Should be able to disconnect from a database')
    test.todo('Should be able to run a query')
})